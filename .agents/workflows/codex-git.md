---
description: Inspect, stage, commit, push, tag, or prepare release/changelog work safely.
---

When the user runs `/codex-git <request>`, use the `codex-git-workflow` skill:

1. Inspect status and diffs.
2. Stage only relevant files.
3. Use the project commit convention or Conventional Commits.
4. Avoid secrets and unrelated user changes.
5. Run relevant verification when feasible.
6. Commit or push only as requested.
7. Report branch, commit, and remote.
